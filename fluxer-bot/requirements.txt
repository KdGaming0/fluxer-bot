fluxer.py>=0.4.1
discord.py>=2.3.0
asyncpraw>=7.7.0
aiohttp>=3.9.0
beautifulsoup4

# cogs/ocr_triggers.py additionally requires the `tesseract` BINARY on PATH
# (not a pip package):
#   Debian/Ubuntu: apt install tesseract-ocr
#   Arch:          pacman -S tesseract tesseract-data-eng
